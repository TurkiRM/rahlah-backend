{
  "name": "rahlah-backend",
  "version": "1.0.0",
  "description": "Rahlah shuttle backend API — matching engine, real accounts (captain/customer/supervisor), push notifications, WebSocket live updates. The customer/captain/supervisor frontends live in their own repos and call this API.",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^4.19.2",
    "web-push": "^3.6.7",
    "ws": "^8.18.0"
  }
}
